# TODO
- Test the installation of a local package in a pipeline [x]
- Remove the dependency on Stack and scan for pipelines?
- Test multi targets / environments
- Figure out how to deal with .gitignore that prevents sync with workspace
- Test streaming operations and Laktory root (dbfs)
